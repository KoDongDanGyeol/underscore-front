import { categoryKey } from "@/queries/map/category"

export {
  EnumCategoryCode,
  type TypeGetCategoryKidId,
  type TypeGetCategoryParams,
  type TypeGetCategoryResult,
} from "@/queries/map/category"

export const mapKey = {
  category: categoryKey,
}
